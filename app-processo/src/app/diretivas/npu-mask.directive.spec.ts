import { NpuMaskDirective } from './npu-mask.directive';

describe('NpuMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new NpuMaskDirective();
    expect(directive).toBeTruthy();
  });
});
